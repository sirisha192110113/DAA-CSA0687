#include<stdio.h>
int main()
{
	int i,num,temp=0;
	printf("enter number");
	scanf("%d",&num);
	for(i=2;i<=num/2;i++)
	{
		if(num%i==0)
		{
			temp++;
			break;
		}
	}
	if(temp==0 && num!=1)
	{
		printf("%d is prime number",num);
	}
	else
	{
		printf("%d is not prime ",num);
	}
 return 0;	
	
}
